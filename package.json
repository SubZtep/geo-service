{
  "name": "geo-service",
  "version": "1.0.3",
  "packageManager": "bun@1.3.14",
  "private": true,
  "scripts": {
    "build": "bun build --minify --target bun --outdir ./dist src/index.ts",
    "dev": "bun run --hot src/index.ts",
    "lint": "biome check --config-path=.",
    "lint:fix": "biome check --config-path=. --write --unsafe",
    "test": "bun test"
  },
  "dependencies": {
    "@maxmind/geoip2-node": "^7.1.0",
    "hono": "^4.12.30",
    "p-queue": "^9.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@types/bun": "^1.3.14"
  }
}
